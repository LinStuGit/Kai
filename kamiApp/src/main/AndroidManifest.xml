<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:label="Kami Console"
        android:icon="@android:drawable/sym_def_app_icon"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <!-- Shizuku: lets elevated shell commands run without root -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Extension interface: agent pushes features via adb broadcast -->
        <receiver
            android:name=".ExtensionReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.kami.app.ext.ADD" />
                <action android:name="com.kami.app.ext.REMOVE" />
                <action android:name="com.kami.app.ext.LIST" />
                <action android:name="com.kami.app.ext.CLEAR" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
